{
  "name": "schoolmanager-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Pruebas E2E (Playwright) de SchoolManager. Aisladas del frontend Angular. NUNCA apuntan a producción por defecto (ver guardrail en playwright.config.ts).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
